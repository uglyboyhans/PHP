index.php{
	if admin{
		func[]={showArticleList,readArticle,createArticle,//done
                        manageArticle};
	}
	else {
		func[]={showArticleList,readArticle};
	}
}

createArticle.php{//done
	save to mysql directly;
}

readBlog.php{
	get article from mysql;//done
	get comment from mysql;//done
        get reply from mysql;//done
	if(admin){
		if(manage){
                    edit or delete;//use option
                }
                if(reply){
                    reply;
                }
	}
	if(visiter){//to do
		if(comment){//done
			
		}		
	}
}
regiest.php{
    save to mysql;
    username unique;
}
login.php{
	use session;
}

logout.php{
	session;
}

manage.php{
	if(edit){
		title and article=now's;
		update mysql;
	}
	if(delete){
		delete from mysql;
		delete comment from mysql;
	}
	if(reply){
		use like msg board;
	}
}


mysql.table[]={blog,comment,user};
blog[]={id,name,title,article};//name sets unique;id is primary key;
comment[]={id,visitor_name,content,reply,time};
user[]={username(unique),password};