index.php{
	if admin{
		func[]={showArticleList,readArticle,manageArticle,createArticle};
	}
	else {
		func[]={showArticleList,readArticle};
	}
}

createArticle.php{//actually done
	save to mysql directly;
}

readArticle.php{
	get article from mysql;
	get comment from mysql;
        get reply from mysql;
	if(admin){//can edit
		if(edit){
			edit.php;
		}
                if(reply){
                    edit.php;
                }
	}
	if(visiter){// can comment
		if(comment){
			use like msg board;//seems difficult...~_~...
		}		
	}
}
regiest.php{
    save to mysql;
    username unique;
}
login.php{
	use session;
}

logout.php{
	session;
}

edit.php{
	if(edit){
		title and article=now's;
		update mysql;
	}
	if(delete){
		delete from mysql;
		delete comment from mysql;
	}
	if(reply){
		use like msg board;
	}
}


mysql.table[]={blog,comment,user};
blog[]={id,name,title,article};//name sets unique;id is primary key;
comment[]={id,visitor_name,content,reply,time};
user[]={username(unique),password};